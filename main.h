#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>

int _putchar(int c);
int print_int(int num);
int _printf(const char *format, ...);
int _puts(char *str);
int print_binary(unsigned int num);
#endif
